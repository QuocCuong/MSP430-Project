#include <msp430.h>
int main(void)
{
	WDTCTL = WDTPW | WDTHOLD;	// stop watchdog timer
	P1DIR |= BIT0 + BIT1 + BIT2 + BIT3;

	while(1){
	    P1OUT |= BIT0 + BIT1 + BIT2 + BIT;
	    __delay_cycles(1000000);
	}

	return 0;
}
